/**
 * Home / admin CMS previews load from the API (`kind=hero`, `category`, `event`).
 * Demo home config: `Frontend/demo-backup/frontendConfig.js`.
 */

export const HOME_CONFIG = {
  heroBanners: [],
  categories: [],
  events: [],
};
